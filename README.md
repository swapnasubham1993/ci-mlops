# ci-mlops
This project demonstrates the setup and automation of a Continuous Integration (CI) pipeline using GitHub Actions. It ensures that code changes are automatically tested, validated, and built every time a new commit or pull request is pushed to the repository.
